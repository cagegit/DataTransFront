1、delete->update功能，界面和后台都未添加此功能。
2、指定数据库连接需完善，如果指定一个已经存在的数据库名字，不会重新记录该数据库信息，默认使用已有的数据库信息。
3、capture和loader高级选项，配合前端做修正。
4、过滤界面表达式范例，添加支持新列值、旧列值, 以及不连接数据库时，表达式范例。
5、File_load和mqpublisher组件。
6、规则刷新表和trigger添加到dipse.sql脚本里。
7、etl批量配置
   1）配置对象时，添加输入框，可以直接输入：用户.表名。
   2）提供某条规则配置了哪些对象功能。
   3）在某个etl上，可以查看某一个规则集里规则的详细信息。
8、loader 当前错误、历史错误展示问题。